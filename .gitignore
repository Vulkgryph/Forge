# Rust build artifacts (shared workspace target/)
/target

# OS / editor cruft
.DS_Store

# Runtime session/conversation state (agent, IDE, and dev-tooling variants)
.forge/
.agent/
.opencode/

# TUI (Bun/TypeScript)

# Local scratch screenshots
Screenshot*.png
*.txt

# Claude Code session runtime state (not project content)
.claude/
